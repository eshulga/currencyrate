export const style = theme => ({
  chartConteiner: {
    minHeight: '250px',
    padding: `${theme.spacing.unit * 2}px 0 0 0`
  },
  select: {
    margin: `0 ${theme.spacing.unit}px`
  },
  tools: {
    padding: `${theme.spacing.unit * 2}px 0 0 0`
  }
})
